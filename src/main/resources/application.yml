spring:
  application:
    name: kuai-da
  # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/kuai_da
    username: root
    password: 1234
server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.xun.kuaida.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn

